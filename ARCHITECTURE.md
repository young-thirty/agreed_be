# Agreed 백엔드 기술 설계 문서

---

## 1. 저장소 구성 방침

프론트엔드(Next.js)와 백엔드(FastAPI)를 별도 저장소로 분리한다.

분리에는 비용이 따른다. 실행 프로세스가 둘로 늘고 CORS와 환경변수 동기화라는 실패 지점이 생긴다. 그럼에도 분리를 택한 것은, 도메인 계층이 프레임워크에 의존하지 않도록 설계돼 있어 이전 비용이 파일 단위 이동에 그쳤기 때문이다. 실제로 이 저장소의 `core/`는 이전 저장소의 `core/`를 언어만 바꿔 옮긴 것이며 규칙은 한 줄도 바뀌지 않았다.

기술적 완성도는 프레임워크의 개수가 아니라 **계층 분리**로 확보한다. 2절의 계층 규칙이 그 수단이다.

---

## 2. 계층 구조

의존 방향은 한 방향으로만 흐른다.

```
app/     ─────▶   core/   ◀─────   infra/
(진입점)         (도메인)          (외부 의존)
             ▲
         models/
      (영속성 — core의 값 객체를 상속)
```

| 계층 | 역할 |
|---|---|
| `app/` | 요청 파싱, 응답 반환, 계층 간 연결 |
| `core/` | 도메인 규칙, 상태 전이, 검증 |
| `models/` | Beanie Document 정의 |
| `infra/` | AI 호출, 입력 어댑터 |

핵심 제약은 하나다. **`core/`는 프레임워크와 외부 환경을 참조하지 않는다.** 이 계층에는 `fastapi`, `beanie`, `pymongo`, `openai`, `os.environ`이 등장하지 않으며, 입력을 받아 출력을 내는 순수 함수와 값 객체만 존재한다.

### 값 객체와 Document의 분리

Beanie `Document`는 `init_beanie()` 이전에는 인스턴스화되지 않는다. 도메인 계층이 Document를 참조하면 계층 규칙 위반일 뿐 아니라 초기화 순서에 따라 애플리케이션이 기동하지 못한다.

따라서 필드 정의는 `core/domain.py`의 값 객체가 소유하고, `models/`의 Document가 이를 상속하여 영속성만 부가한다.

```python
class ContractState(BaseModel):     # core/domain.py
    version: int
    scope: list[str]

class Contract(ContractState, Document):   # models/contract.py
    class Settings:
        name = "contracts"
```

`Contract`는 `ContractState`의 하위 타입이므로 도메인 함수에 그대로 전달된다. 필드를 중복 정의하지 않으면서 의존 방향이 보존된다.

---

## 3. 저장소 선택

MongoDB를 채택한다. 근거는 두 가지다.

**첫째, 스키마 변경 비용이다.** 본 프로젝트는 화면 설계가 진행되는 동안 도메인 타입이 계속 변경되는 상태다. 관계형 저장소에서는 필드 추가마다 마이그레이션이 필요하고, 이 과정이 개발 기간 중 오류의 주요 발생원이 된다. 문서 저장소에서는 필드 추가가 기존 문서에 영향을 주지 않으며, 누락된 필드는 Pydantic의 기본값이 처리한다.

**둘째, 자료 구조의 성격이다.** 요구사항은 `evidence` 배열과 `basis` 판별 유니온을 포함하는 중첩 구조이며, 계약은 `scope` 배열을 포함한다. 관계형 모델에서는 JSON 컬럼이나 별도 테이블로 우회해야 하고, 어느 쪽이든 조회 시 타입 검증이 유실된다. 본 시스템에는 조인이 필요한 질의가 존재하지 않으므로 관계형 모델의 이점이 발휘될 지점이 없다.

**한계도 명시한다.** 요구사항을 계약에 반영할 때 `Requirement`와 `Contract` 두 문서가 함께 변경되므로 멱등 키, 프로젝트별 버전 unique index와 충돌 처리가 필요하다. 로그인 도입 이후 모든 조회·수정은 세션 사용자의 `ownerId` 범위 안에서만 수행한다. 여러 프로젝트 전환 시 두 문서에 `projectId`를 추가하는 정확한 순서는 `PRODUCT_API_DESIGN.md`를 따른다.

ODM으로는 Beanie를 사용한다. Pydantic 모델이 곧 문서 스키마이므로 API 응답 모델과 저장 스키마를 별도로 정의하지 않는다. 정의가 한 곳에 모이면 수정해야 할 지점이 하나로 줄어든다.

---

## 4. 실행 환경

| 항목 | 선택 |
|---|---|
| 웹 프레임워크 | FastAPI |
| 언어 | Python 3.12 |
| 스키마 검증 | Pydantic v2 |
| 저장소 | MongoDB |
| ODM | Beanie 2.x |
| AI | DeepSeek (`deepseek-chat`) |

Beanie 2.x는 motor가 아닌 pymongo의 비동기 클라이언트(`AsyncMongoClient`)를 사용한다. 1.x 기준 예제와 초기화 코드가 다르므로 주의를 요한다.

---

## 5. 확장 계획

현재 구조는 개발 기간의 제약에 따른 임시 조치가 아니라, 이후 단계로의 이전 비용을 최소화하도록 의도된 구조이다. 전제는 2절의 계층 제약이다.

**입력 경로의 확장이 진행 중이다.** `feat/#6`에서 확인한 슬랙·지메일 OAuth와 조회 로직을 `infra/integrations/`로 옮긴다. provider 응답은 먼저 채널 공통 DTO로 정규화하고, 이후 `infra/ingest/`가 AI 입력 발화로 바꾼다. 도메인 계층은 provider token과 REST 응답을 알지 못한다.

**사용자 개념을 도입했다.** 자체 이메일·비밀번호 로그인은 서버측 opaque session을 사용하고, Google·Slack OAuth는 로그인과 별도의 연동 권한으로 취급한다. 브라우저에는 HttpOnly 세션 쿠키만 두며 provider token은 암호화해 MongoDB에 저장한다. 소유권은 요청 body가 아니라 세션에서 정한다.

**모델 교체는 `infra/llm/client.py` 한 파일에 국한된다.** 도메인 계층은 어떤 모델이 응답했는지 알지 못하며, 검증 계층은 응답의 출처와 무관하게 동일하게 동작한다.

세 경우를 관통하는 공통점은 **도메인 계층이 보존된다는 점**이다. 저장소, 입력 경로, 모델은 교체 가능한 주변부로 취급하며, 변경 비용은 해당 어댑터의 크기에 비례한다.

---

## 6. 인공지능 적용 방식과 근거

### 6.1 적용 범위

인공지능은 두 지점에 적용한다. 고객과의 대화에서 새로 발생한 요구사항을 추출하고 그 성격을 분류하는 것, 그리고 추출된 요구사항이 기존 계약의 어느 조항에 근거하는지 대조하는 것이다. 프로젝트 sync 후 요청 판정과 자료 분류는 BackgroundTasks로 구현되어 있다.

반대로 인공지능을 적용하지 않는 영역을 명확히 구분한다. 요구사항의 상태 전이는 규칙으로 확정되어 있으므로 코드가 처리한다. 계약 반영 여부의 판단은 합의 성립이라는 사실 확인에 해당하므로 추론의 대상이 아니다. 금액과 일정의 결정은 사용자의 판단 영역이며, 시스템이 대신하지 않는다.

검색 증강 생성(RAG)과 미세조정은 채택하지 않았다. 계약 문서는 분량이 짧아 전체를 문맥에 포함할 수 있으며, 이를 분할하여 일부만 참조할 경우 대조에 필요한 조항이 누락되어 정확도가 오히려 저하된다. 미세조정은 학습 자료가 확보되지 않은 현 단계에서 적용할 근거가 없다.

### 6.2 문제 유형의 규정

본 시스템이 다루는 과제는 대화에서 구조화된 상태를 추출하여 지속적으로 갱신하는 문제, 즉 대화 상태 추적(Dialogue State Tracking)에 해당한다.

공개 벤치마크에서 대규모 언어모델에 과제 전체를 위임하는 종단간 방식은 낮은 정확도를 보이며, 추출·분류·검증을 분리하여 각 단계를 개별적으로 처리하는 방식이 유의미하게 높은 성능을 기록한다. 또한 화자가 다수인 대화에서는 단일 화자 대비 성능 저하가 관찰된다. 본 시스템이 다루는 대화는 발주자와 수행자가 함께 참여하는 다자 대화이므로, 과제 난이도는 표준 벤치마크보다 높다고 보아야 한다.

이러한 근거에 따라 본 시스템은 언어모델에 과제 전체를 위임하지 않고, 6.3절의 다단계 검증 구조를 채택하였다.

### 6.3 검증 구조

인공지능의 출력을 신뢰하지 않는다는 전제 아래 다섯 계층의 검증을 둔다.

| 계층 | 구현 위치 | 내용 |
|---|---|---|
| L0 발화 분할 | `infra/ingest/paste.py` | 대화를 발화 단위로 나누고 식별자를 부여 |
| L1 스키마 강제 | `infra/llm/schemas.py`, `extract.py` | JSON mode + Pydantic 재검증, 실패 시 1회 재시도 |
| L2 근거 검증 | `core/grounding.py` | 인용문의 원문 존재 여부 대조, 부분 수용 |
| L3 상태 전이 검증 | `core/state_machine.py` | 도달 불가능한 상태는 '미확정'으로 강등 |
| L4 사람 승인 | `core/contract_ops.py` | 합의되지 않은 요구사항은 반영 불가 |

**L0**에서 부여한 인덱스는 이후 단계에서 모델의 출력을 원문에 대응시키기 위한 기준점이 된다. 화면의 원문 하이라이트 또한 이 인덱스에 의존한다.

**L1**은 모델의 출력 형식을 JSON mode로 강제하고, 수신한 결과를 Pydantic 스키마로 재검증한다. 제안 가능한 상태 목록은 `LLM_PROPOSABLE`을 그대로 참조하므로 목록을 두 곳에 기술하지 않는다. 검증에 실패한 경우 오류 내용을 포함하여 1회에 한해 재요청하며, 반복 실패 시 폴백 경로로 전환한다.

**L2**는 모델이 제시한 근거 인용문이 원문에 실제로 존재하는지 대조한다. 존재하지 않는 인용은 모델이 생성한 허구로 간주하여 해당 항목을 폐기한다. 이때 전체 결과를 폐기하지 않고 항목 단위로 처리하는 부분 수용 방식을 적용하여, 일부 오류가 전체의 실패로 확대되지 않도록 한다. 한국어 대화에서는 모델이 인용을 옮기며 공백과 따옴표를 변형하는 사례가 빈번하므로, 정규화 후 비교한다.

**L3**은 모델이 제안한 상태가 이전 상태로부터 도달 가능한지 검증한다. 도달 불가능한 경우 요청을 거부하는 대신 '미확정' 상태로 강등한다. 거부는 화면에 아무 정보도 남기지 않지만, 강등은 사용자가 판단할 대상을 남기기 때문이다.

**L4**는 사용자 승인을 확인한다. 합의가 성립하지 않은 요구사항은 계약에 반영될 수 없으며, 이 검사를 우회하는 경로는 시스템 내에 존재하지 않는다.

### 6.4 설계상의 특징

본 설계의 특징은 모델의 불확실성을 은폐하지 않고 도메인의 상태로 표현했다는 점에 있다.

일반적으로 인공지능을 적용한 제품은 모델이 확신하지 못하는 상황에서 그럴듯한 결과를 제시하거나 오류를 반환한다. 그러나 계약이라는 도메인에서 아직 결정되지 않은 상태는 예외가 아니라 정상적인 상태이다. 마찬가지로 모델이 확신하지 못하는 상황 또한 정상적인 동작에 해당한다.

본 시스템은 이 두 가지를 동일한 상태로 대응시켰다. 모델이 요구사항의 성격을 확정하지 못하면 그 결과를 '미확정' 상태로 기록하며, 이 상태는 그 자체로 사용자에게 판단을 요청하는 신호가 된다. 불확실성을 감추지 않는 설계가 곧 사용자의 판단 영역을 보존하는 장치로 기능한다.

### 6.5 폴백 전략

로컬 환경에서 안정적으로 동작하는 것이 목표다. 네트워크가 끊겨도 서비스가 중단되지 않아야 한다.

```
1) 캐시 확인    고정 시연 시나리오면 저장된 결과를 즉시 반환
2) 실제 호출    타임아웃 8초
3) 안전 실패    빈 결과 + 사용자 안내
```

타임아웃 없는 호출을 만들지 않는다. 클라이언트의 `max_retries`는 0으로 설정한다. SDK 기본 재시도(2회)와 타임아웃이 중첩되면 최악의 경우 24초가 소요되기 때문이며, 재시도는 L1이 검증 실패 시 1회만 수행한다.

### 6.6 성능 측정

요구사항 분류의 정확도는 별도로 구축한 평가 집합을 통해 측정한다. 대화 문장과 정답 상태를 사전에 표기한 자료를 사용하며, 측정 결과와 함께 오분류 사례의 유형을 함께 제시한다. 오분류 유형을 명시하는 것은 측정이 실제로 수행되었음을 보이는 근거이며, 이후 프롬프트 개선의 기준이 된다.

---

## 7. API 설계

모든 응답은 다음 두 형태 중 하나를 취한다.

```
{ "ok": true,  "data": ... }
{ "ok": false, "error": "사용자가 그대로 읽을 수 있는 문장" }
```

`error`에는 스택 트레이스나 내부 식별자를 포함하지 않는다. HTTP 상태 코드는 정상 200, 잘못된 입력 400, 대상 없음 404, 서버 오류 500만 사용한다.

| 메서드 | 경로 | 역할 |
|---|---|---|
| GET | `/api/health` | 기동 확인 |
| POST | `/api/auth/signup` | 자체 회원가입 + 세션 시작 |
| POST | `/api/auth/login` | 자체 로그인 + 세션 시작 |
| POST | `/api/auth/logout` | 세션 폐기 |
| GET | `/api/auth/me` | 로그인 사용자 조회 |
| GET | `/api/email/connect` | Gmail OAuth 시작 |
| GET | `/api/email/callback` | Gmail OAuth callback |
| POST | `/api/email/messages` | Gmail 원문 조회·정규화 |
| GET | `/api/slack/connect` | Slack OAuth 시작 |
| GET | `/api/slack/callback` | Slack OAuth callback |
| POST | `/api/slack/workspaces` | 연결된 워크스페이스 조회 |
| POST | `/api/slack/channels` | 채널 목록 조회 |
| POST | `/api/slack/messages` | 채널 메시지 조회 |
| POST | `/api/slack/thread` | 스레드 조회 |
| GET | `/api/contract` | 현재 계약 조회 (최신 버전) |
| POST | `/api/contract` | 최초 계약 등록 |
| POST | `/api/contract/apply` | L4 승인 게이트. 계약 버전 증가 |
| POST | `/api/analyze` | 대화 분석. L0~L3 수행 |
| GET | `/api/requirements` | 요구사항 목록 |
| GET | `/api/requirements/{id}/allowed` | 현재 상태에서 선택 가능한 전이 |
| POST | `/api/requirements/{id}/transition` | 사람 조작 상태 전이 |

FastAPI가 `/docs`에 OpenAPI 문서를 자동 생성한다. 별도의 API 문서를 작성하지 않는다.

OAuth 시작·callback과 Slack 파일 스트림은 브라우저 redirect/파일 표시를 위한 GET 예외다. 나머지 데이터 API는 공통 `{ok, data}` / `{ok, error}` 응답을 사용한다. 상세 수집·AI 구조는 [DATA_AI_PIPELINE.md](./DATA_AI_PIPELINE.md)에 둔다.

계약은 버전마다 새 문서를 생성한다. 갱신이 아니라 추가이므로 이전 버전이 보존되며, 어떤 요구사항으로 인해 계약이 어떻게 변경되었는지 추적할 수 있다.
