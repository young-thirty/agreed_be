# Agreed 화면 흐름과 설계 근거

> 티켓 하나가 들어와 답변이 나가기까지, 화면이 어떤 API를 부르고 왜 그렇게
> 나눴는지 적는다. 화면 코드는 `young-thirty/agreed_dev`(Next.js), API는 이 저장소다.
>
> 도메인 정책은 [DOMAIN_SPEC.md](./DOMAIN_SPEC.md), 에이전트 구조는
> [AI_AGENTS.md](./AI_AGENTS.md), 확정 DTO는
> [PRODUCT_API_DESIGN.md](./PRODUCT_API_DESIGN.md)에 있다.

이 문서는 **2026-08-26 기준 실제 코드를 대조해** 썼다. 화면이 요구하지만 백엔드가
아직 채우지 못하는 값은 숨기지 않고 7절에 모아 뒀다.

---

## 1. 한 줄 흐름

```text
고객 메시지  →  분석  →  답변
```

화면 좌측 상단에 그대로 적혀 있는 문장이고, 이것이 제품의 전부다.
**대시보드가 아니라 티켓이 첫 화면이다.** 프리랜서가 아침에 여는 이유는
"어떤 프로젝트가 있나"가 아니라 "내가 답해야 할 게 뭐가 남았나"이기 때문이다.

```text
사이드바
├ 티켓      ← 기본 진입. 처리해야 할 것이 먼저 보인다
└ 프로젝트  ← 계약 단위. 티켓이 속한 맥락
```

---

## 2. 티켓 목록 (이미지 1 좌측)

`GET /api/tickets?projectId=&status=`

상단 탭은 다섯 개다.

| 탭 | 뜻 | 백엔드 |
|---|---|---|
| **처리 필요** | 답 안 한 메시지가 있는 티켓 | 프론트가 `pending`이 있는 항목만 거른다 |
| 전체 | 상태 무관 | `status` 없이 호출 |
| Active / Done / Reject | 티켓 상태 | `status=Active|Done|Reject` |

목록 카드 하나에 들어가는 것: 티켓 코드(`TCK-91`), 상태 배지, 상대 시각,
제목, 새 메시지 미리보기, 프로젝트명 · 채널, 그리고 **처리 단계**다.

### 처리 단계 (`workStage`)

카드 우측 하단의 `답변 준비됨` · `추가 정보 필요` · `분석 대기`가 이 값이다.
티켓 상태(Active/Done/Reject)와 **다른 축**이다.

- 티켓 상태 = 이 요청을 받을 것인가 (사람이 정한다)
- 처리 단계 = 지금 내가 뭘 해야 하는가 (데이터 상태에서 계산된다)

둘을 합치면 "Active인데 답변까지 다 쓴 티켓"과 "Active인데 아직 분석도 안 된
티켓"을 구분할 수 없다. 목록에서 무엇부터 손댈지가 안 보인다.

### 근거: 왜 상태 필터가 서버에 있고 처리 필요는 프론트에 있는가

`status`는 문서에 저장된 값이라 인덱스를 태울 수 있다
(`(ownerId, projectId, ticketStatus)`). `처리 필요`는 티켓·메시지·판단 세
문서를 조합해야 나오는 파생값이다. 시연 MVP는 전체 배열을 돌려주므로 프론트가
거르는 편이 API를 하나 덜 만든다.

---

## 3. 티켓 상세 (이미지 1 우측)

`GET /api/tickets/{ticketId}` — 화면 한 장에 필요한 것을 한 번에 준다.

```text
[프로젝트명 >]                                   [답변 필요]
카카오 소셜 로그인 추가                          [Active ▾]
TCK-91 · 기능 요청 · 마지막 업데이트 28분 전
─────────────────────────────────────────────
요구사항    (미확정) 카카오 OAuth 로그인 추가
현재 상태   고객이 카카오 로그인 추가를 요청해 만들어진 티켓이다. …
```

- 우측 상단 드롭다운으로 **사람이 직접** 상태를 바꾼다
  → `PATCH /api/requests/{requestId}/ticket-status`
- `요구사항`은 `ticket.requirement`, `현재 상태`는 `ticket.currentSummary`

### 근거: 왜 상세 조회가 여러 문서를 합쳐 주는가

이 화면 한 장이 티켓·원문·판단·자료·프로젝트 다섯 문서를 본다. 각각 API를
따로 두면 화면 진입에 왕복이 다섯 번이고, 그 사이 화면이 조각조각 채워진다.
MongoDB는 조인이 없는 대신 문서가 중첩 구조를 담을 수 있으므로, **읽기 화면
단위로 묶어 내려주는 편**이 맞다. 쓰기는 문서별로 나뉘어 있다.

### 근거: 상태 전이를 AI가 하지 않는 이유

"이 요청에 대응이 끝났는가"는 메시지만 봐서 알 수 없다. 실제로 작업했는지,
고객이 납득했는지는 대화 밖에서 일어난다. 자동화하면 열려 있어야 할 티켓이
닫히고, 그게 곧 놓친 요청이 된다. 드롭다운 하나로 둔다.

---

## 4. 네 단계로 읽는 상세 본문

번호가 붙은 네 블록이 순서대로 쌓인다. **AI가 하는 일과 사람이 하는 일의
경계가 3번에서 갈린다.**

### 1 고객 메시지

원문 그대로다. 제목, 채널(Email/Slack), 시각, 보낸 사람, 본문.
보낸 사람 표기는 채널마다 다르다 — 메일은 이름·주소, Slack은 표시 이름이다.
Slack은 사용자 이메일을 못 가져오는 경우가 있어 `senderExternalId`를 함께 남긴다.

### 2 AI 분석

`ticket.solution`과 `analysis` payload가 그린다.

```text
한 메시지에 3개의 요청이 섞여 있습니다. 비용과 일정에 대한 판단이 필요합니다.

한 메시지에서 3개의 요청을 찾았습니다
  1  기능 요청   카카오 로그인 추가
  2  일정 문의   이번 주 완료 가능 여부
  3  계약 문의   기존 견적 포함 여부

범위          ⚠ 기존 계약 범위 밖일 가능성이 높습니다
개발          · 이메일 로그인은 구현 완료
              · OAuth 구조가 없어 추가 구현이 필요
일정          ⚠ 진행 중인 예약 알림 작업과 영향 영역이 겹쳐 …
사용자 판단 필요  · 추가 비용   · 완료 예정일
```

**네 항목은 고정이다.** `범위 / 개발 / 일정 / 사용자 판단 필요`는 값이
없어도 자리를 비우지 않고 "확인하지 못했다"를 채운다.

근거: 항목이 상황마다 나타났다 사라지면 사람이 매번 무엇이 빠졌는지 찾아야
한다. 고정된 네 칸이면 눈이 같은 자리를 보고, 빈 칸 자체가 "이건 확인이
안 됐구나"라는 정보가 된다. 각 항목은 서로 다른 에이전트가 만든다 —
범위는 계약 대조, 개발은 저장소 탐색, 일정·판단 필요는 영향 분석과 작업 가능
여부다([AI_AGENTS.md](./AI_AGENTS.md) 4절).

#### 개발 상황 확인

```text
</> 개발 상황도 확인할까요?              [개발 상황 확인]
    로그인 관련 구현이 어디까지 됐는지 GitHub에서 확인합니다.
    d-booking/server
```

연결된 저장소를 얕게 clone해 읽고 지운다. **버튼을 따로 둔 이유**는 clone이
느리고(5~25초) 모든 티켓에 필요하지도 않아서다. 디자인 수정 티켓에 저장소를
뒤질 이유가 없다.

### 3 근거 (이미지 3 상단)

> 근거 — 눌러서 원문을 확인할 수 있습니다

| 종류 | 예 |
|---|---|
| 제안서·계약서 | `D사_예약서비스_제안서.pdf` |
| 관련 Ticket | `TCK-88 로그인 기능` |
| GitHub | `d-booking/server · AuthService, UserEntity` |
| GitHub | `PR #31 예약 알림 문자 연동` |

**모델이 옮겨 적은 인용은 코드가 원문과 다시 대조한다**(`core/grounding.py`).
지어낸 인용이면 그 근거를 버리고, 초록 판정이었다면 주황으로 내린다. 눌러서
원문을 볼 수 있어야 하므로 존재하지 않는 인용이 남아 있으면 화면이 깨진다.

### 4 내 판단 (이미지 3 하단)

> 여기서부터는 사람이 정합니다

```text
이 요구를 티켓에 반영할까요?
  TCK-91 카카오 소셜 로그인 추가                    ● Active

[🔗 이 티켓에 반영]  [＋ 별도 티켓으로 분리]  [🚫 반영하지 않음]

AI는 TCK-88 로그인 기능도 함께 봤습니다. 요구사항 확정과 상태 변경은 사람만
합니다. 누르기 전에는 아무것도 바뀌지 않습니다.
```

`POST /api/requests/{requestId}/decision` — `handling`이 `link` / `create` /
`ignore` 셋이다.

**"누르기 전에는 아무것도 바뀌지 않습니다"가 이 제품의 핵심 문장이다.**
AI는 여기까지 재료만 만들고 멈춘다. 별도 티켓으로 분리하면 새 티켓 코드가
발급된다(이미지 3의 `TCK-91` → 이미지 4의 `TCK-92`).

판단을 바꾸면 앞선 판단이 만든 임시 티켓만 정리한다
(`_remove_created_target`). 사람이 직접 만든 티켓은 건드리지 않는다.

### 5 답변 초안 (이미지 4)

판단을 정하면 열린다. 처리 방식이 정해지기 전에는 초안을 만들지 않는다 —
받아들일지 거절할지에 따라 문장이 통째로 달라지기 때문이다.

#### 결정 항목

```text
결정이 필요합니다
AI가 정할 수 없는 값입니다. 여기 넣은 값이 아래 답변 초안에 그대로 들어갑니다.

추가 비용  [예: 300000]      완료 예정일  [연도. 월. 일.]
```

`analysis.decisionFields`가 무엇을 물을지 정한다. 지금은 계약 범위 밖
(`EXTRA_REQUEST`) 판정일 때만 금액·납기를 묻는다.

#### 초안과 빈칸

```text
[기본] [조금 더 친절하게] [짧게] [단호하게]        ⟳ 다시 생성

3. 비용
추가 비용은 (추가 비용 미정)입니다.

아직 정하지 않은 값이 ( ) 로 남아 있습니다. 위에서 채우면 문장에 들어갑니다.
```

**금액과 날짜를 AI가 지어내지 않는다.** 사람이 확정하지 않은 값은 `( )`로
남고, 위 입력란을 채우면 그 자리에 들어간다. 빈칸이 남아 있어도 보낼 수는
있다 — 무엇이 안 정해졌는지 화면이 알려주고, 보낼지는 사람이 정한다.

말투 4종은 `REPLY_STYLES` 레지스트리에서 온다. 목록을 늘리는 것은 dict에 한
줄 더하는 일이고 스키마를 고치지 않는다. **말투가 바꾸는 것은 어떻게 말할지뿐**
이고, 무엇을 말할지는 3번의 판단과 결정 항목이 정한다.

`답변 보내기` → `POST /api/requests/{requestId}/mark-sent`
**실제 메일·Slack 발송은 아직 없다.** 보냈다는 표시만 남는다(7-2 참고).

### 지난 대화

이 티켓에 쌓인 고객 메시지와 보낸 답변이 시간순으로 붙는다.
받은 것은 상대 이름·채널·시각·본문, 보낸 것은 `보낸 답변`으로 표시된다.

---

## 5. 완료된 티켓 (이미지 5)

상단 구성은 같다. 다른 점은 **답할 메시지가 없다는 것**이다.

```text
요구사항 / 현재 상태          ← 같음
지금 답할 메시지는 없습니다. 새 고객 메시지가 오면 여기에서 이어집니다.
지난 대화                      ← 바로 이어짐
```

AI 분석·내 판단·답변 초안 세 블록이 통째로 빠진다. 처리할 것이 없는데 판단
버튼을 띄우면 사람이 누를 게 있는 줄 알고 들여다본다.

**Done이어도 새 메시지가 오면 다시 이어진다.** 티켓을 닫는 것은 "지금까지는
정리됐다"는 뜻이지 대화가 끝났다는 뜻이 아니다.

---

## 6. 프로젝트

사이드바의 두 번째 항목이다. 티켓이 속한 계약 단위이고, 상세 화면 상단의
빵부스러기(`D사 예약 서비스 구축 >`)로도 들어간다.

`GET /api/projects` · `GET /api/projects/{projectId}`

> 이 절은 화면 설명을 아직 받지 못했다. 프로젝트 단위 흐름이 정해지면
> 여기에 이어 적는다.

---

## 7. 현재 배선 상태와 남은 차이

`GET /api/tickets/{id}`의 `analysis` payload를 화면과 대조한 결과다. 아래 7-1과
7-3은 `9878832`에서 연결됐다. 남은 차이는 관련 티켓의 정확도, 개발 현황 호출
시점, 실제 외부 발송이다.

### 7-1. `analysis` 필드 배선 완료

| 필드 | 현재 값의 출처 | 화면 |
|---|---|---|
| `intents` | 같은 원문에서 추출된 형제 `ClientRequest` | 이미지 2의 "3개의 요청을 찾았습니다" 목록 |
| `missingInfo` | `solution.feasibility.requiredHumanInput` | "사용자 판단 필요: 추가 비용, 완료 예정일" |
| `devContext` | GitHub source link와 `solution.developmentStatus` | 개발 상황 카드와 저장소 이름 |

솔루션이 아직 생성되지 않았으면 개발·가능 여부 값은 `확인하지 못했습니다`로
나온다. 화면 진입 시 `POST /api/requests/{id}/solution`을 한 번 호출하고 저장된
결과를 다시 조회해야 실제 값이 채워진다.

### 7-2. 근거 종류는 연결됐지만 관련 티켓 선별은 보완 필요

`evidence`는 `message`, `document`, `ticket`, `github` 네 종류를 만든다.
GitHub 근거는 `solution.developmentStatus.relatedPaths`와 `relatedRefs`를 사용한다.

다만 `ticket` 근거는 아직 티켓 매칭 에이전트가 확정한 결과가 아니라 같은 프로젝트의
최근 티켓 최대 3개다. 무관한 티켓을 "관련 Ticket"으로 표시할 수 있으므로,
매칭 결과 ID를 저장해 그대로 조회하거나 유사도 기준을 추가해야 한다.

### 7-3. 고정 네 항목 배선 완료

`fields`는 `범위 / 개발 / 일정 / 사용자 판단 필요` 네 칸을 항상 반환한다.
값이 없을 때도 칸을 없애지 않고 `확인하지 못했습니다` 또는 `없음`을 넣는다.

| 화면 항목 | 솔루션 필드 |
|---|---|
| 범위 | `scopeDecision` |
| 개발 | `developmentStatus.currentState` |
| 일정 | `impactAnalysis.existingFeatureImpact` |
| 사용자 판단 필요 | `feasibility.requiredHumanInput` |

여기서 `일정`은 AI가 날짜를 정하는 칸이 아니라 기존 기능 영향과 테스트 범위를
보여주는 **일정 영향**이다. 실제 완료 예정일은 사람이 `decisionFields.dueDate`에
입력한다.

### 7-4. 답변이 실제로 나가지 않는다

`mark-sent`는 보냈다는 표시만 남긴다. Gmail 권한이 `gmail.readonly`뿐이라
발송 스코프가 없고, Slack도 쓰기 권한을 받지 않았다.

시연에서는 초안을 복사해 직접 보내는 것으로 갈음한다. 발송을 붙이려면 OAuth
스코프를 다시 받아야 해서 콘솔 재설정이 필요하다.

### 7-5. 프론트 로그인이 백엔드 세션을 만들지 않는다

프론트 `/signup`이 프로필을 localStorage에만 저장하고 `/api/auth/signup`을
부르지 않는다. 지금 화면이 도는 것은 브라우저에 이미 세션 쿠키가 있어서다.
**다른 브라우저·시크릿 창에서 열면 401로 빈다.**

백엔드 API는 그대로 두고 프론트가 붙일 예정이다.

### 7-6. 개발 확인 버튼과 답변 초안의 호출 시점

현재 `POST /api/requests/{id}/solution`은 계약 대조와 GitHub clone을 한 번에
실행한다. 화면의 "개발 상황 확인" 버튼을 엄밀히 유지하려면 이 API를 그 버튼에서
처음 호출하거나, 개발 현황만 별도 refresh하는 API로 나눠야 한다.

또한 솔루션 종합 결과에 기본 `replyDraft`가 포함되지만 화면은 사람이 처리 방식과
금액·납기를 정하기 전에는 이를 숨긴다. 결정 이후 말투별 초안은
`POST /api/requests/{id}/reply-draft`로 다시 만들어 `TicketDecision.drafts`에 저장한다.

---

## 8. 사람이 쥐는 스위치

AI가 어디까지 하고 사람이 어디서 정하는지 한눈에.

| 단계 | 주체 |
|---|---|
| 메시지에서 요청을 찾아내기 | AI |
| 기존 티켓에 붙일지 판단 | AI (제안까지) |
| 계약 범위 안팎 판정 | AI (근거는 코드가 재검증) |
| 개발 현황·영향·작업 가능 여부 | AI |
| **이 요구를 티켓에 반영할지** | **사람** |
| **티켓 상태(Active/Done/Reject)** | **사람** |
| **추가 비용·완료 예정일** | **사람** |
| 답변 초안 작성 | AI |
| **답변 발송** | **사람** |
| **계약 반영** | **사람** (`apply_to_contract`) |

계약을 바꾸는 통로는 `core/contract_ops.py`의 `apply_to_contract` 하나뿐이고
그 앞에 합의 여부 검사가 있다. **이 검사를 우회하는 경로를 만들지 않는다.**
