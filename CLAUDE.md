# CLAUDE.md

> Agreed 백엔드 저장소의 작업 규약이다. 사람과 AI 에이전트가 공통으로 지킨다.
> 작업 시작 전에 반드시 읽는다.
>
> 프론트엔드는 별도 저장소(`young-thirty/agreed_dev`, Next.js)에 있다.
> 이 저장소는 API만 담당한다.

---

## 1. 프로젝트 개요

**서비스명: Agreed**

계약이 체결된 이후 고객과 오가는 대화에서 새로 발생한 요구사항과 합의의 변화를 추적해, 계약 범위·일정·비용을 항상 최신 상태로 유지하는 서비스다.

핵심 원칙은 하나다.

> **AI는 무엇이 바뀌었는지 관리하고, 사람은 바꿔도 되는지 판단한다.**

금액·일정·수락 여부는 사람이 결정한다. 시스템이 대신 결정하지 않는다.

**"AI가 계약서를 자동 수정한다"고 표현하지 않는다.** 정확한 표현은 "AI가 변경분 초안을 만들고, 사람이 승인하면 반영된다"이다. 코드도 그렇게 생겼다 — 계약을 바꾸는 함수는 하나뿐이고 그 앞에 합의 여부 검사가 있다. 말과 코드가 어긋나면 안 된다.

기획 배경과 시연 시나리오는 [HANDOFF.md](./HANDOFF.md)에, 설계 근거는 [ARCHITECTURE.md](./ARCHITECTURE.md)에 있다.

---

## 2. 절대 규칙

1. **이 저장소는 백엔드 전용이다.** 화면 코드를 여기에 두지 않는다.
2. **인증·로그인이 없다.** 사용자 개념 자체가 없다. 회원가입 화면이 기획에 있더라도 서버는 사용자를 구분하지 않는다.
3. **테스트 코드를 작성하지 않는다.** 검증은 실행으로 한다.
4. **AI에게 금액·일정·수락 여부를 결정시키지 않는다.** LLM 출력 스키마에 확정 금액 필드를 두지 않는다.
5. **`core/`는 외부 의존을 갖지 않는다.** 3절 참조.
6. **비밀 키는 `.env`에만 둔다.** 응답으로 내보내지 않는다.

---

## 3. 계층 구조

의존 방향은 한쪽으로만 흐른다.

```
app/     ─────▶   core/   ◀─────   infra/
(진입점)         (도메인)          (외부 의존)
             ▲
         models/
      (영속성 — core의 값 객체를 상속)
```

| 계층 | 하는 일 | 하지 않는 일 |
|---|---|---|
| `app/` | 요청 파싱, 응답 반환, 계층 연결 | 비즈니스 규칙 판단 |
| `core/` | 도메인 규칙, 상태 전이, 검증 | 외부 호출, 프레임워크 사용 |
| `models/` | Beanie Document 정의 | 도메인 규칙 판단 |
| `infra/` | AI 호출, 입력 어댑터 | 도메인 규칙 판단 |

### 절대 규칙

**`core/`에는 다음이 등장하면 안 된다.**

```
fastapi   beanie   pymongo   openai   os.environ
```

`core/`는 입력을 받아 출력을 내는 순수 함수의 모음이다. 이 규칙이 지켜지면 프레임워크나 저장소를 바꿔도 도메인 로직이 그대로 살아남는다.

### `core/`는 Document를 import하지 않는다

이건 계층 규칙일 뿐 아니라 **실제로 앱이 죽는 문제**다. Beanie `Document`는 `init_beanie()`가 돌기 전에는 인스턴스 생성조차 안 되고 `CollectionWasNotInitialized`를 던진다.

그래서 이렇게 나눈다.

```python
# core/domain.py — 순수 값 객체
class ContractState(BaseModel):
    version: int
    scope: list[str]
    ...

# models/contract.py — 값 객체를 상속해 영속성만 얹는다
class Contract(ContractState, Document):
    class Settings:
        name = "contracts"
```

`Contract`는 `ContractState`의 하위 타입이므로 `core/` 함수에 그대로 넘길 수 있다. 필드를 두 번 정의하지 않으면서 `core/`는 Beanie를 모른다.

**상속 순서에 유의한다. 값 객체가 앞, `Document`가 뒤다.**

### 모듈 로드 시점에 Document를 만들지 않는다

전역에 목 데이터를 `Requirement(...)`로 선언하는 코드를 두면 앱이 아예 뜨지 않는다. Document 인스턴스는 요청 처리 중에만 만든다.

---

## 4. 폴더 구성

```
app/
├─ main.py                FastAPI 앱 + lifespan(init_beanie)
├─ response.py            ok() / fail() 응답 규약
└─ api/                   라우트
   ├─ analyze.py
   ├─ contract.py
   └─ requirements.py

core/                     순수 TypeScript가 아닌 순수 Python. 외부 의존 0
├─ domain.py              값 객체 단일 원천
├─ state_machine.py       상태 전이 규칙 (L3)
├─ grounding.py           근거 검증 (L2)
└─ contract_ops.py        승인 게이트 + 변경분 계산 (L4)

models/                   Beanie Document
├─ contract.py
└─ requirement.py

infra/
├─ ingest/                입력 어댑터 경계
│  └─ paste.py            붙여넣기 → Utterance[]
└─ llm/
   ├─ client.py           DeepSeek 클라이언트
   ├─ prompts.py          프롬프트 (코드와 분리)
   ├─ schemas.py          출력 스키마 (L1)
   ├─ extract.py          파이프라인 L1→L2→L3
   └─ fallback.py         시연 폴백
```

프롬프트는 코드에 문자열로 박지 않고 `infra/llm/prompts.py`에 모은다. 튜닝할 때 로직을 건드리지 않기 위해서다.

---

## 5. 기술 스택

| 항목 | 선택 |
|---|---|
| 웹 프레임워크 | FastAPI + uvicorn |
| 언어 | Python 3.11+ |
| 검증 | Pydantic v2 |
| DB | MongoDB |
| ODM | Beanie 2.x |
| AI | DeepSeek (`deepseek-chat`) |

### 알아둘 것

- **Beanie 2.x는 motor를 쓰지 않는다.** `pymongo.AsyncMongoClient`를 쓴다. 인터넷의 옛 예제는 대부분 `AsyncIOMotorClient`인데 그건 Beanie 1.x다.
- **DeepSeek은 OpenAI 호환이다.** `openai` 패키지에 `base_url="https://api.deepseek.com"`만 바꿔 쓴다.
- **구조화 출력은 JSON mode를 쓴다.** function calling 대신이다. 어차피 받은 결과를 Pydantic으로 다시 검증하므로(L1), 실패 지점이 적은 쪽을 택했다.
- **새 라이브러리를 추가하기 전에 팀에 묻는다.**

---

## 6. AI 설계 원칙

### 6.1 역할 분담

| 주체 | 담당 |
|---|---|
| **AI** | 대화에서 무엇이 있는지 찾아내고 분류한다 |
| **코드** | 승인되지 않은 것이 반영되지 않도록 막는다 |
| **사람** | 받아줄지, 얼마에, 언제까지를 결정한다 |

AI에게 상태 전이나 계약 반영 여부를 맡기지 않는다. 그것은 규칙이지 추론이 아니다.

### 6.2 5계층 검증

**전제: LLM 출력을 신뢰하지 않는다. 매 계층에서 검증하고, 실패하면 안전한 쪽으로 강등한다.**

```
대화 텍스트
   ↓
[L0] 발화 분할        infra/ingest/paste.py
   ↓
[L1] 스키마 강제      infra/llm/schemas.py + extract.py
   ↓
[L2] 근거 검증        core/grounding.py
   ↓
[L3] 상태 전이 검증   core/state_machine.py
   ↓
[L4] 사람 승인        core/contract_ops.py
   ↓
계약 갱신
```

**L0 발화 분할** — 대화를 통째로 넘기지 않는다. 발화 단위로 쪼개고 인덱스를 부여한다. 이 인덱스가 있어야 L2의 근거 검증과 화면의 원문 하이라이트가 가능하다.

**L1 스키마 강제** — 프롬프트로 "JSON만 출력해"라고 부탁하는 데서 그치지 않는다. JSON mode로 받고 Pydantic으로 다시 검증한다. `proposedStatus`는 `LLM_PROPOSABLE`만 받으므로 모델이 '합의'·'완료'·'거절'을 내면 검증에서 걸린다. 검증 실패 시 **오류 내용을 덧붙여 1회만 재시도**한다. 두 번째도 실패하면 빈 결과로 넘어간다. 무한 재시도는 하지 않는다.

**L2 근거 검증** — 모델이 내놓은 인용문이 원문에 실제로 존재하는지 확인한다. 없으면 그 항목을 버린다. **부분 수용을 원칙으로 한다.** 5건 중 1건이 실패하면 그 1건만 버리고 나머지 4건은 살린다. 한국어 대화에서는 모델이 인용을 옮기며 공백·따옴표를 바꾸는 일이 잦으므로, 정규화 후 비교한다.

**L3 상태 전이 검증** — 모델이 제안한 상태가 이전 상태에서 도달 불가능하면 **거부하지 않고 '미확정'으로 강등한다.** 거부하면 화면에 아무것도 남지 않지만, 강등하면 사람이 판단할 거리가 남는다.

**L4 사람 승인 게이트** — 계약을 변경하는 통로는 `apply_to_contract` 하나뿐이며, 그 안에 승인 검사가 있다.

```python
if requirement.status != "합의":
    raise ValueError(...)
```

**이 검사를 우회하는 다른 경로를 만들지 않는다.**

### 6.3 폴백 전략

```
1) 캐시 확인    고정 시연 시나리오면 저장된 결과를 즉시 반환 (네트워크 안 탐)
2) 실제 호출    타임아웃 8초
3) 안전 실패    빈 결과 + 사용자 안내. 예외를 화면까지 던지지 않는다
```

- **타임아웃 없는 호출을 만들지 않는다.**
- **`max_retries=0`으로 둔다.** SDK 기본 재시도와 타임아웃이 겹치면 최악 24초까지 간다. 재시도는 L1이 직접 1회만 한다.
- **예외를 삼키지 않되, 화면을 깨뜨리지도 않는다.** 라우트에서 잡아 안내 문구로 바꾼다.

---

## 7. 응답 규약

모든 API는 POST와 JSON을 쓰고(조회는 GET) 다음 형태로 통일한다.

```python
{ "ok": True,  "data": ... }
{ "ok": False, "error": "사용자가 그대로 읽을 한국어 문장" }
```

`app/response.py`의 `ok()` / `fail()`을 쓴다. **이 규약은 프론트엔드와 공유한다.** 바꾸면 양쪽이 함께 깨진다.

`error`에 스택 트레이스나 내부 식별자를 노출하지 않는다.

```
나쁨:  "오류가 발생했습니다."
좋음:  "대화 내용을 분석하지 못했습니다. 다시 시도해 주세요."
```

HTTP 상태 코드는 정상 200, 잘못된 입력 400, 없음 404, 서버 오류 500만 쓴다.

### 필드 이름은 camelCase다

프론트엔드의 `types/index.ts`가 이미 `dueDate`, `amountDelta`, `utteranceIndex` 같은 이름으로 응답을 기대한다. Python에서 snake_case를 쓰면 직렬화 별칭 설정이 한 겹 더 필요하고, 그게 어긋나면 조용히 필드가 사라진다. **이름을 그대로 맞추는 쪽을 택했다.** 파이썬답지 않아 보여도 바꾸지 않는다.

---

## 8. 작업 원칙

### 8.1 단순함이 먼저다

문제를 푸는 최소한의 코드만 쓴다. 미래를 대비한 코드는 쓰지 않는다.

- 요청받지 않은 기능은 만들지 않는다.
- 한 곳에서만 쓰는 코드에 추상화 계층을 만들지 않는다.
- 일어날 수 없는 상황에 대한 예외 처리를 넣지 않는다.

### 8.2 최소한만 건드린다

- 옆에 있는 코드, 주석, 포맷을 개선하지 않는다.
- 망가지지 않은 것을 리팩터링하지 않는다.
- **내 변경 때문에** 쓰이지 않게 된 import는 정리한다. 원래부터 안 쓰이던 것은 건드리지 않는다.

기준은 하나다. **바뀐 모든 줄이 요청 사항으로 직접 추적되어야 한다.**

### 8.3 검증 가능한 목표로 바꾼다

테스트 코드는 쓰지 않으므로, 실행해서 확인할 수 있는 조건으로 정의한다.

```
1. [작업] → 확인: [확인 방법]
2. [작업] → 확인: [확인 방법]
```

---

## 9. Git 컨벤션

### 브랜치

- **`main`이 기본 브랜치다.** 지금(해커톤 스프린트)은 `main`에 직접 커밋한다.

### 작업 순서

```
1. 원격을 최신화한다          → git fetch origin main && git merge --ff-only origin/main
2. 작업하고 커밋한다
3. 푸시 직전 다시 확인한다     → git fetch origin main && git merge origin/main
4. 푸시한다                   → git push origin main
```

**3번을 건너뛰지 않는다.** 여러 명이 같은 브랜치에 커밋하므로, 푸시 전에 원격이 앞서 있으면 먼저 받아 합친 뒤 푸시한다. `--force`는 쓰지 않는다.

### 양식

```
타입: 작업내용요약
```

| 타입 | 용도 |
|---|---|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `chore` | 설정, 의존성, 잡무 |
| `docs` | 문서 |

요약은 한국어로 쓴다.

---

## 10. 하지 않는 것

- 테스트 코드
- 로그인, 인증, 사용자 관리
- 별도 서버 분리, 마이크로서비스
- 국제화
- 과도한 로딩 상태와 에러 바운더리
- 요청받지 않은 리팩터링

---

## 11. 백엔드 체크리스트

작업을 마치기 전에 확인한다.

- [ ] `core/`에 fastapi·beanie·openai·os.environ이 없는가
- [ ] `core/`가 `models/`를 import하지 않는가
- [ ] LLM 호출에 타임아웃이 있는가 (`max_retries=0`인가)
- [ ] LLM 출력을 Pydantic으로 검증했는가
- [ ] 검증 실패 시 전체를 버리지 않고 부분 수용하는가
- [ ] 계약을 변경하는 경로가 승인 검사를 지나는가
- [ ] 에러 문구가 사용자가 읽을 문장인가
- [ ] 응답 필드가 camelCase인가
- [ ] API 키가 응답에 섞이지 않는가

---

## 12. AI 에이전트에게

- 주석과 커밋 메시지는 한국어로 쓴다.
- 기존 파일을 통째로 다시 쓰지 않는다. 필요한 부분만 고친다.
- 새 의존성을 추가하기 전에 묻는다.
- `core/domain.py`를 수정하는 변경은 먼저 알린다. 프론트엔드가 함께 참조한다.
- 에러 메시지는 사용자가 읽을 문장으로 쓴다.
- 확신이 없으면 코드를 쓰기 전에 묻는다.
